# Input length from user
length = float(input("Enter the length of the square: "))

# Calculate perimeter
perimeter = 4 * (length)

# Display results
print("The perimeter (length around) of the square is:", perimeter)